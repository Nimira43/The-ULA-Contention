import './css/styles.css'

document.querySelector('#app').innerHTML = `
<div class='temp-container'>
  <h1 class='logo-font'>
    The ULA Contention
  </h1>
  <p class='main-font'>
    A ULA is a type of chip where the manufacturer (Ferranti, in the Spectrum's case) produced a generic, partially-configured logic device.</p>
</div>
`
